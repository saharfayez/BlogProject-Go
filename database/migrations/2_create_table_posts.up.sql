create table posts (
                       id int AUTO_INCREMENT,
                       title varchar(50),
                       content varchar(50),
                       user_id int,
                       PRIMARY KEY (id)
);
